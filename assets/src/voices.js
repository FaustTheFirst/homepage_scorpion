const normalVoice = document.getElementById('normalAudio')
    .addEventListener('click', () => document.getElementById('normalSound').play());

const funVoice = document.getElementById('funAudio')
    .addEventListener('click', () => document.getElementById('funSound').play());
